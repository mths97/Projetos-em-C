#include <stdio.h>
#include <stdlib.h>

int main()
{
    int vetor[10] = {6, 4, 1, 9, 8, 0, 5, 7, 3, 2};
    int i;
    void ordemCrescente(int vetor[], int n);

    ordemCrescente(vetor, 10);

    for (i=0; i<10; ++i)
    {
        printf ("%i ", vetor[i]);
    }

    return 0;
}

void ordemCrescente (int vetor[], int n)
{
    int i, j, aux;

    for (i=0; i<n; ++i)
    {
        for(j= i+1; j<n; ++j)
        {
            if (vetor[i] > vetor[j])
            {
                aux = vetor[i];
                vetor[i] = vetor[j];
                vetor[j] = aux;
            }
        }
    }
}

//////////////////////////////////////////////////////
Na função ordemCrescente():
int i é o primeiro número no vetor;
int j é o número que vem depois do primeiro (i+1);

O programa irá verificar se cada número i é maior que o seu sucessor j
Se for, irá inverter de posição através da variável aux
